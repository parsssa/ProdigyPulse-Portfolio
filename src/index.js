import React from "react";
import ReactDOM  from "react-dom";
import App from './App';

//App component is going to be rendered inside a div in our ../public/index.html called 'root'
ReactDOM.render(<App />, document.getElementById('root'));